#include "main.h"
/**
 * _islower -checks if a in its lowecase
 * @c: The charscter to be checked
 *
 * Return: 1 if the character is in uppercase otherwise 0
 */
int _islower(int)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (1);
}
